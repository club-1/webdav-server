<?php

/** @var string Only used for mails addresses for now. */
$host = 'club1.fr';
/** @var string UID of the anonymous user. */
$anonymous = 'anonymous';
/** @var string The currently logged in user. */
$user = $_SERVER['AUTHENTICATE_UID'];
/** @var string Current user's home directory. */
$home = "/home/$user";
